version: '3.8'

services:
  mediahub-api:
    build: .
    image: ${REGISTRY_PREFIX:-local}/mediahub:latest
    container_name: mediahub-server
    restart: unless-stopped
    ports:
      - "51733:51733"
    volumes:
      # Consolidated Host Mappings
      - ./shared_media:/app/shared_media:ro # Media is read-only for safety
      - ./data:/app/data # Consolidates DB, thumbs, logs, temp, HLS and sprites
      - ./.env:/app/.env
    environment:
      - REDIS_URL=redis://redis:6379/0
    networks:
      - mediahub-net
    depends_on:
      - redis

  redis:
    image: redis:alpine
    container_name: mediahub-redis
    command: redis-server --appendonly yes # Enable AOF persistence
    restart: unless-stopped
    volumes:
      - redis_data:/data
    networks:
      - mediahub-net

networks:
  mediahub-net:
    driver: bridge

volumes:
  redis_data:
    driver: local
